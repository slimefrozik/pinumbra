{
  "name": "pinumbra",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Pinumbra — a 3D first-person polar-bear-hunting survival shooter built with Three.js.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "lint": "eslint \"src/**/*.js\""
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "vite": "^5.4.10"
  }
}
